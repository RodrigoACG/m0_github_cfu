## Git Commands I know so far

git init (do this everytime i start off a new repository)
git add <filename>
git commit -m " message of what I have done"
git status ( checks status)
git diff (checks changes)
git log(see all commits)